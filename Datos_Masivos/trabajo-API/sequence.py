class Sequence:
    def __init__(self, seq_id, version, query, desc, molecule, seq):
        self.seq_id = seq_id
        self.version = version
        self.query = query
        self.desc = desc
        self.molecule = molecule
        self.seq = seq